import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
	header_icon: {
		flexDirection: "row",
		alignItems: "center",
		marginTop: 30,
	},
	header_back: {
		marginLeft: 10,
	}
});